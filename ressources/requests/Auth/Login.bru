meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/member/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "login": "{{username}}",
    "password": "{{password}}"
  }
}

script:post-response {
  const body = res.getBody();
  const jwt = body.token
  
  bru.setVar("authToken", jwt)
}
